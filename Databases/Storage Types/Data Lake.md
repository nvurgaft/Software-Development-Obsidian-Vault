Data Lakes are storage options used to store raw or binary data, text files, csv, json, backups, images, etc..

Data Lakes allow simple querying to quickly fetch that data.

Data Lakes are a common approach for "Big Data" and unstructured data.

Data partitioning is a consideration for best performance. 