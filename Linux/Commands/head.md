# Head
#linux 

`head` prints the first n lines of a file

`head` does the exact opposite of [[tail|tail]]

Basic usage: 
```sh
head -n <number of lines> <path>
```

Example

```sh
head -n 5 /path/to/file.txt
```
Parameters

`-n` number of lines to print starting from the start of the file