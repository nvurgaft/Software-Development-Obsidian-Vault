`kubectl` is a command line tool used to communicate with Kubernetes' [[Control Plane]] using it's API. `kubectl` comes with the installation of Kubernetes and Minikube.

Common commands

Display version information
```sh
kubectl version
```

Shows usage and commands
```sh
kubectl --help
```

A Quick reference guide can be found at https://kubernetes.io/docs/reference/kubectl/quick-reference/