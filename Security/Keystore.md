A keystore is a repository of security certificates (either authorization certificates, symmetric keys or public key certificates with corresponding private keys) used for [[TLS]] encryption. 

In Java the keystore has the `.jks` extension.