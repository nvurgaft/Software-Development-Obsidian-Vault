A reverse proxy is a server that sites in front of backend servers and services and forwards [[Client]] requests to them on behalf of the application.

Clients think they’re talking directly to your application, but they’re actually talking to the reverse proxy.

An [[API Gateway]] can be considered a sort of reverse proxy although it is more complex and cover more use cases.