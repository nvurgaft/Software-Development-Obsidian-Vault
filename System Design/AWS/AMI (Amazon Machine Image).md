The Amazon Machine Image is an AWS tool used to create and deploy virtual machines within the [[EC2 (Elastic Compute Cloud)]].

The AMI includes 

1. A template for the root values of the instance, that is, the OS
2. Launch permission that control which AWS accounts can use the AMI to launch instances
3. A block device mapping that specifies the volumes to attach to the instance when it's launched.